[project]
name = "ai-router"
version = "0.1.0"
description = "Production AI Router for Local LLM"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "pydantic",
    "pydantic-settings",
    "loguru",
    "python-dotenv",
]